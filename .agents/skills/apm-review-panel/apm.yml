name: apm-review-panel
version: 0.1.0
description: Multi-persona advisory review panel for a labelled pull request -- fans out to specialist personas + a CEO synthesizer, posts one recommendation comment, never gates merge.
author: Microsoft
license: MIT
type: skill

# Stand-alone primitive: no transitive APM deps. The skill body honors
# the consumer repo's linting / encoding rules at runtime by activating
# whichever instructions the consumer has installed.
dependencies:
  apm: []
  mcp: []
